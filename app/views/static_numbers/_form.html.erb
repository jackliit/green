<%= form_for(@static_number) do |f| %>
  <% if @static_number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@static_number.errors.count, "error") %> prohibited this static_number from being saved:</h2>

      <ul>
      <% @static_number.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :number %><br>
    <%= f.number_field :number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
